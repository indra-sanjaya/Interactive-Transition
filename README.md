# Interactive-Transition
A small interactive list demo showcasing DOM manipulation, accessibility-friendly controls, and modern CSS View Transition animations using vanilla HTML, CSS, and JavaScript.

📄 index.html

Defines the HTML structure for an interactive list UI.
Includes list items with accessible “Move to Top” buttons and Font Awesome icons, serving as the entry point of the application.

🎨 style.css

Handles layout, styling, and animations for the list interface.
Uses modern CSS features such as View Transitions, nested rules, and keyframe animations to create smooth, dynamic item movements and visual effects.

⚙️ javascript.js

Implements interactive behavior for the list.
Manages button states, moves selected list items to the top, and coordinates View Transition API animations with graceful fallback support for unsupported browsers.
